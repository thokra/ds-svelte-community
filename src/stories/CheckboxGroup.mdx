import { Stories, Primary, Meta, Controls } from "@storybook/addon-docs";

import CheckboxGroup from "./CheckboxGroup.stories.svelte";

<Meta of={CheckboxGroup} />

# CheckboxGroup

We use Checkbox and CheckboxGroup when users need to be able to select multiple options.

Read more about this component in the [Aksel documentation](https://aksel.nav.no/komponenter/core/checkboxgroup).

<Primary />

## Properties

### CheckboxGroup

<Controls />

<Stories includePrimary={false} />

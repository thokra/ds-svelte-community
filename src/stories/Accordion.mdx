import { ArgTypes, Story, Canvas, Meta, Controls } from "@storybook/addon-docs";

import Accordion from "./Accordion.stories.svelte";

import { AccordionItem } from "$lib";

<Meta of={Accordion} />

# Accordion

Accordion is related sections with interactive headers that allow users to open or close the view of
the content below.

Read more about this component in the [Aksel documentation](https://aksel.nav.no/komponenter/core/accordion).

<Canvas>
	<Story id="ds-svelte-accordion--default" />
</Canvas>

## Properties

### Accordion

<ArgTypes />

### AccordionItem

<ArgTypes of={AccordionItem} />

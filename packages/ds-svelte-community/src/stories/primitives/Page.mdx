import { ArgTypes, Primary, Meta, Controls, Stories } from "@storybook/addon-docs";

import Page from "./Page.stories.svelte";
import PageBlock from "$lib/components/primitives/Page/PageBlock.svelte";

<Meta of={Page} />

# Page

Page makes it easier to build the top-level layout for your app. This includes placing the footer, setting the maximum width, and centering content blocks.

Read more about this component in the [Aksel documentation](https://aksel.nav.no/komponenter/primitives/page).

<Primary height="300px" />

## Properties

### Page

Controls will not automatically update the preview above.
Select the `Default` story in the sidebar to see the changes.

<Controls />

### PageBlock

<ArgTypes of={PageBlock} />

<Stories includePrimary={false} />

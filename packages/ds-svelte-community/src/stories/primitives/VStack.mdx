import { ArgTypes, Primary, Meta, Controls, Stories } from "@storybook/addon-docs";

import VStack from "./VStack.stories.svelte";

<Meta of={VStack} />

# VStack

VStack is a layout component for vertically grouping elements together with correct spacing.

Read more about this component in the [Aksel documentation](https://aksel.nav.no/komponenter/primitives/vstack).

<Primary />

## Properties

### VStack

<Controls />

<Stories includePrimary={false} />
